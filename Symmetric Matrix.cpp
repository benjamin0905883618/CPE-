#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;

int main(){
    int n;
    while(cin >> n){
        for(int i = 0;i < n;i++){
            string temp;
            cin >> temp;
            cin >> temp;
            int t;
            cin >> t;
            vector<int> row;
            row.assign(t,0);
            vector<vector<int>> matrix;
            matrix.assign(t,row);
            for(int j = 0;j < t;j++){
                for(k = 0;k < t;k++)
                    cin >> v[i][j];
            }



        }
    }
}
